---
'@ai-sdk/harness-codex': patch
---

Allow deterministic Codex harnesses to reconstruct a redacted live bridge token from the resumed sandbox identity while keeping random-token resume state validation strict.
