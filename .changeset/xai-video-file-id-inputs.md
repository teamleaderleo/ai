---
'@ai-sdk/xai': patch
---

Add xAI Files API `referenceImageFileIds` support for reference-to-video generation while preserving existing URL, inline-data, and first-class `inputReferences` behavior.
